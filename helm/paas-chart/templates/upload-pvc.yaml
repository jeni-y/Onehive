apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.APP_NAME }}-upload-pvc
  namespace: {{ .Values.NAMESPACE }}
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 5Gi